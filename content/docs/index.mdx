---
title: Introduction
description: Welcome to the PeersTech documentation
---

Tools for decentralized communication. No central message server, accounts, or
project-operated message database. Optional relay nodes and a node directory
make communication across NATs possible.

## Projects

<Cards>
  <Card
    title="Peers"
    description="An end-to-end encrypted messenger. Peer-to-peer over libp2p."
    href="/docs/peers"
  />
  <Card
    title="Directory API"
    description="A live registry of relay nodes. Client integration is planned, not yet wired."
    href="/docs/dir-api"
  />
  <Card
    title="Pterodactyl egg"
    description="Deploy relay nodes through Pterodactyl after building the custom runtime image."
    href="/docs/ptero-egg"
  />
</Cards>

## How it works

```text
graph TB
    subgraph Clients
        A[Peers App]
        B[Peers App]
    end

    subgraph P2P Network
        C[libp2p Mesh]
        D[Kademlia DHT]
        E[Gossipsub]
    end

    subgraph Relay Nodes
        F[Node 1]
        G[Node 2]
        H[Node N]
    end

    A -->|E2E Encrypted| C
    B -->|E2E Encrypted| C
    C --> D
    C --> E
    F --> C
    G --> C
    H --> C

    subgraph Manual Setup
        O[Operator copies PEERS_NODES line]
    end

    F --> O
    O --> A
    O --> B

    subgraph Directory
        I[Directory API]
    end

    F -->|Register + Heartbeat| I
    G -->|Register + Heartbeat| I
    H -->|Register + Heartbeat| I
    A -.-|Planned: fetch nodes| I
```

Peers find each other over the public IPFS DHT. That solves discovery. Relay nodes solve reachability: both NAT'd peers dial the same node, reserve a circuit, and chat. DCUtR then tries to upgrade them to a direct connection.

Today, an operator copies the `PEERS_NODES=` line from `peers --node` and hands it to both users, who paste it into `PEERS_NODES` or `nodes.json`. The Directory API already accepts registrations and heartbeats from nodes. Client fetching is the planned next step, not yet wired.

## Design principles

- Self-certifying identities. Ed25519 keys embedded in peer IDs.
- Serverless by default. No canonical node list ships with Peers, since that would make those nodes a central dependency.
- NAT traversal through Circuit Relay v2 and DCUtR hole punching.
- Runs on Raspberry Pi, old laptops, and cheap VPSs. A node uses 30 to 60 MB of RAM.
